{
  "Razorpay": {
    // SECRET — never committed. Loaded at runtime from Key Vault (Razorpay--KeyId /
    // Razorpay--KeySecret) via the service's managed identity. These stay empty in source control.
    "KeyId": "",
    "KeySecret": ""
  }
}
